SELECT DISTINCT p1.user_id
FROM amazon_transactions p1
JOIN amazon_transactions p2
  ON p1.user_id = p2.user_id
WHERE DATEDIFF(p2.created_at, p1.created_at) BETWEEN 1 AND 7
AND p1.created_at = (
    SELECT MIN(t.created_at)
    FROM amazon_transactions t
    WHERE t.user_id = p1.user_id
);
